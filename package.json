{
  "name": "ai-crypto-analyst",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/api",
    "apps/web"
  ],
  "scripts": {
    "dev": "concurrently -k -n ai,api,web -c magenta,cyan,green \"npm run dev:ai\" \"npm run dev:api\" \"npm run dev:web\"",
    "start": "npm run dev",
    "dev:api": "npm run db:up && npm run redis:up && npm run start:dev -w @aca/api",
    "db:up": "node scripts/postgres.mjs start",
    "db:down": "node scripts/postgres.mjs stop",
    "db:status": "node scripts/postgres.mjs status",
    "redis:up": "node scripts/redis.mjs start",
    "redis:down": "node scripts/redis.mjs stop",
    "redis:status": "node scripts/redis.mjs status",
    "dev:web": "npm run dev -w @aca/web",
    "dev:ai": "py -3 apps/ai/server.py",
    "build": "npm run build -w @aca/api && npm run build -w @aca/web"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
